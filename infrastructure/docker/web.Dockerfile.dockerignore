# Scoped to the web image's build context (repo root) via Docker's
# "<dockerfile>.dockerignore" convention -- does not affect the api build.
apps/api
networks
docs
.superpowers
.git
apps/web/node_modules
apps/web/.next
apps/web/tests
apps/web/tsconfig.tsbuildinfo
